import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "注册 - 简语 Simplingua",
  description: "注册简语学习平台账号。",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
